import { ScrollView } from "react-native";
import AccountCreationForm from "@/components/accountCreation"

export default function Create() {
    return(
        <ScrollView>
          <AccountCreationForm />
        </ScrollView>
      );
}